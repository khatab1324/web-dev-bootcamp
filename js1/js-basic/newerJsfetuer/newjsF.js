// ---------------------------------------------default params ---------------------------------
//                              the old way
function rollDie(num) {
  return Math.floor(Math.random() * num) + 1;
}
